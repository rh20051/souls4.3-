[gd_resource type="Environment" load_steps=8 format=3 uid="uid://0pesm0nbq1bn"]

[sub_resource type="Shader" id="Shader_c2hg0"]
code = "
// NOTE: Shader automatically converted from Godot Engine 4.1.2.stable's ProceduralSkyMaterial.

shader_type sky;
render_mode use_debanding;

uniform vec4 sky_top_color : source_color = vec4(0.385, 0.454, 0.55, 1.0);
uniform vec4 sky_horizon_color : source_color = vec4(0.646, 0.656, 0.67, 1.0);
uniform float sky_curve : hint_range(0, 1) = 0.15;
uniform float sky_energy = 1.0; // In Lux.
uniform sampler2D sky_cover : filter_linear, source_color, hint_default_black;
uniform vec4 sky_cover_modulate : source_color = vec4(1.0, 1.0, 1.0, 1.0);
uniform vec4 ground_bottom_color : source_color = vec4(0.2, 0.169, 0.133, 1.0);
uniform vec4 ground_horizon_color : source_color = vec4(0.646, 0.656, 0.67, 1.0);
uniform float ground_curve : hint_range(0, 1) = 0.02;
uniform float ground_energy = 1.0;
uniform float sun_angle_max = 30.0;
uniform float sun_curve : hint_range(0, 1) = 0.15;
uniform sampler2D cloud_texture;
uniform sampler2D cloud_texture2;
uniform vec2 cloud_scale;
uniform float cloud_distance;
uniform vec2 cloud_direction;
uniform vec2 cloud_direction2;
uniform float cloud_speed;
uniform float cloud_speed2;

void sky() {
	float v_angle = acos(clamp(EYEDIR.y, -1.0, 1.0));
	float c = (1.0 - v_angle / (PI * 0.5));
	vec3 sky = mix(sky_horizon_color.rgb, sky_top_color.rgb, clamp(1.0 - pow(1.0 - c, 1.0 / sky_curve), 0.0, 1.0));
	sky *= sky_energy;

	if (LIGHT0_ENABLED) {
		float sun_angle = acos(dot(LIGHT0_DIRECTION, EYEDIR));
		if (sun_angle < LIGHT0_SIZE) {
			sky = LIGHT0_COLOR * LIGHT0_ENERGY;
		} else if (sun_angle < sun_angle_max) {
			float c2 = (sun_angle - LIGHT0_SIZE) / (sun_angle_max - LIGHT0_SIZE);
			sky = mix(LIGHT0_COLOR * LIGHT0_ENERGY, sky, clamp(1.0 - pow(1.0 - c2, 1.0 / sun_curve), 0.0, 1.0));
		}
	}

	if (LIGHT1_ENABLED) {
		float sun_angle = acos(dot(LIGHT1_DIRECTION, EYEDIR));
		if (sun_angle < LIGHT1_SIZE) {
			sky = LIGHT1_COLOR * LIGHT1_ENERGY;
		} else if (sun_angle < sun_angle_max) {
			float c2 = (sun_angle - LIGHT1_SIZE) / (sun_angle_max - LIGHT1_SIZE);
			sky = mix(LIGHT1_COLOR * LIGHT1_ENERGY, sky, clamp(1.0 - pow(1.0 - c2, 1.0 / sun_curve), 0.0, 1.0));
		}
	}

	if (LIGHT2_ENABLED) {
		float sun_angle = acos(dot(LIGHT2_DIRECTION, EYEDIR));
		if (sun_angle < LIGHT2_SIZE) {
			sky = LIGHT2_COLOR * LIGHT2_ENERGY;
		} else if (sun_angle < sun_angle_max) {
			float c2 = (sun_angle - LIGHT2_SIZE) / (sun_angle_max - LIGHT2_SIZE);
			sky = mix(LIGHT2_COLOR * LIGHT2_ENERGY, sky, clamp(1.0 - pow(1.0 - c2, 1.0 / sun_curve), 0.0, 1.0));
		}
	}

	if (LIGHT3_ENABLED) {
		float sun_angle = acos(dot(LIGHT3_DIRECTION, EYEDIR));
		if (sun_angle < LIGHT3_SIZE) {
			sky = LIGHT3_COLOR * LIGHT3_ENERGY;
		} else if (sun_angle < sun_angle_max) {
			float c2 = (sun_angle - LIGHT3_SIZE) / (sun_angle_max - LIGHT3_SIZE);
			sky = mix(LIGHT3_COLOR * LIGHT3_ENERGY, sky, clamp(1.0 - pow(1.0 - c2, 1.0 / sun_curve), 0.0, 1.0));
		}
	}

	vec4 sky_cover_texture = texture(sky_cover, SKY_COORDS);
	sky += (sky_cover_texture.rgb * sky_cover_modulate.rgb) * sky_cover_texture.a * sky_cover_modulate.a * sky_energy;

	c = (v_angle - (PI * 0.5)) / (PI * 0.5);
	vec3 ground = mix(ground_horizon_color.rgb, ground_bottom_color.rgb, clamp(1.0 - pow(1.0 - c, 1.0 / ground_curve), 0.0, 1.0));
	ground *= ground_energy;

	vec2 sky_uv = EYEDIR.xz / EYEDIR.y;
	vec3 clouds = texture(cloud_texture, (sky_uv*cloud_scale) + (TIME*cloud_direction*cloud_speed*.01)).rgb - texture(cloud_texture2, (sky_uv * cloud_scale) + (TIME * cloud_direction2 * cloud_speed2*.01)).rgb;
	clouds = clouds - smoothstep(0.0, EYEDIR.y, cloud_distance);
	clouds = clamp(clouds,0,1);
	sky += clouds;

	COLOR = mix(ground, sky, step(0.0, EYEDIR.y));



}
"

[sub_resource type="FastNoiseLite" id="FastNoiseLite_c6x15"]
frequency = 0.0982

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_lmqvt"]
seamless = true
noise = SubResource("FastNoiseLite_c6x15")

[sub_resource type="FastNoiseLite" id="FastNoiseLite_vkdt0"]
seed = 3
frequency = 0.0284

[sub_resource type="NoiseTexture2D" id="NoiseTexture2D_j18x8"]
seamless = true
noise = SubResource("FastNoiseLite_vkdt0")

[sub_resource type="ShaderMaterial" id="ShaderMaterial_7lll8"]
shader = SubResource("Shader_c2hg0")
shader_parameter/sky_top_color = Color(0.28, 0.196653, 0.1484, 1)
shader_parameter/sky_horizon_color = Color(0.12, 0.0528, 0.0528, 1)
shader_parameter/sky_curve = 1.0
shader_parameter/sky_energy = 1.0
shader_parameter/sky_cover_modulate = Color(1, 1, 1, 1)
shader_parameter/ground_bottom_color = Color(0.2, 0.169, 0.133, 1)
shader_parameter/ground_horizon_color = Color(0.0313726, 0.215686, 0.00392157, 1)
shader_parameter/ground_curve = 0.0
shader_parameter/ground_energy = 1.35
shader_parameter/sun_angle_max = -19.336
shader_parameter/sun_curve = 0.0
shader_parameter/cloud_scale = Vector2(-0.175, -0.35)
shader_parameter/cloud_distance = -0.15
shader_parameter/cloud_direction = Vector2(0.28, 1.705)
shader_parameter/cloud_direction2 = Vector2(-0.765, 2.64)
shader_parameter/cloud_speed = 0.305
shader_parameter/cloud_speed2 = 0.235
shader_parameter/cloud_texture = SubResource("NoiseTexture2D_lmqvt")
shader_parameter/cloud_texture2 = SubResource("NoiseTexture2D_j18x8")

[sub_resource type="Sky" id="Sky_hc3cq"]
sky_material = SubResource("ShaderMaterial_7lll8")
radiance_size = 1

[resource]
background_mode = 2
background_energy_multiplier = 1.3
sky = SubResource("Sky_hc3cq")
ambient_light_source = 2
ambient_light_color = Color(1, 1, 1, 1)
ambient_light_energy = 0.16
reflected_light_source = 2
ssr_fade_in = 0.000528076
ssr_depth_tolerance = 128.0
ssao_intensity = 0.6
ssil_sharpness = 0.0
sdfgi_energy = 0.245
glow_normalized = true
glow_intensity = 8.0
glow_strength = 2.0
glow_mix = 0.205
glow_hdr_threshold = 0.97
glow_hdr_scale = 4.0
glow_hdr_luminance_cap = 163.95
glow_map_strength = 1.0
fog_mode = 1
fog_light_color = Color(0.315275, 0.311475, 0.291105, 1)
fog_light_energy = 3.24
fog_density = 1.0
fog_height_density = -0.0275
fog_depth_curve = 0.987304
fog_depth_end = 62.0
volumetric_fog_density = 0.653
volumetric_fog_albedo = Color(0.529412, 0.529412, 0.529412, 1)
volumetric_fog_gi_inject = 0.06
volumetric_fog_detail_spread = 6.0
volumetric_fog_ambient_inject = 16.0
volumetric_fog_sky_affect = 0.0
adjustment_enabled = true
